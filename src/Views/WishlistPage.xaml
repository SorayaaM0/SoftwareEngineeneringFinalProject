<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StoreApp.Views.WishlistPage"
             Title="My Collections"
             BackgroundColor="White">

    <VerticalStackLayout Padding="20">
        <Label Text="My Collections" FontSize="28" FontAttributes="Bold" Margin="0,0,0,20"/>

        <CollectionView x:Name="WishlistCollection" EmptyView="No items saved yet!">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10" ColumnDefinitions="Auto, *, Auto">
                        <Image Source="{Binding imageUrl}" WidthRequest="80" HeightRequest="80" Aspect="AspectFit"/>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Margin="15,0">
                            <Label Text="{Binding name}" FontAttributes="Bold" FontSize="16"/>
                            <Label Text="{Binding price, StringFormat='${0:F2}'}" TextColor="Gray"/>
                        </VerticalStackLayout>
                        <Button Grid.Column="2" Text="Remove" Clicked="OnRemoveClicked" CommandParameter="{Binding .}" BackgroundColor="Transparent" TextColor="Red"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>